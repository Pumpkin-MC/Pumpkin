[files]
extend-exclude = ["assets/", "pumpkin-data/src/generated"]

[default.extend-words]
handled = "handled"
received = "received"
ba = "ba"
fo = "fo"
undescore = "undescore"
replaceables = "replaceables"
# Vanilla's own misspelled identifier (Aquifer.java: FLOWING_UPDATE_SIMULARITY), quoted
# verbatim in a source-citation comment so it's grep-able against the decompiled source.
simularity = "simularity"
